applications:
  - name: pal-tracker
    run: chmod +x ./gradlew
    path: build/libs/pal-tracker.jar
    routes:
    - route: pal-tracker-tim.cfapps.io
    env:
      WELCOME_MESSAGE: Hello from the review environment
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'